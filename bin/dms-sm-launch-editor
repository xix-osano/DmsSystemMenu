#!/bin/bash

dms-sm-cmd-present "$EDITOR" || EDITOR=nvim

case "$EDITOR" in
nvim | vim | nano | micro | hx | helix)
  exec setsid alacritty "$EDITOR" "$@"
  notify-send "Editing config file" "$1"
  ;;
*)
  exec setsid "$EDITOR" "$@"
  notify-send "Editing config file" "$1"
  ;;
esac
